# project-handoff 三种交接方式对比测试报告

> 实验时间：2026-09-22 ｜ 测试对象：project-handoff skill v1.0.2 ｜ 方式：模拟双角色对照实验

## 一、实验设置

**测试项目**：原生 JS 贪吃蛇小游戏（故意做成"半成品"）——能玩（移动/吃食物/变长），但有明确烂尾点：撞墙判定的 `gameOver()` 未实现被注释（"不死蛇" bug）、无最高分、无加速、无得分板 UI。

**上家 agent A 的隐藏上下文**（只存在于"剧本"中，不写进项目文件——这正是真实交接的痛点）：
- 用户明确说过："**贪吃蛇要什么暂停，死了重来就行；最高分得有**"（→ 不能加暂停键）
- bug 根因：判定逻辑没错，缺的只是 gameOver 实现（→ 不该重复排查）
- 认可的下一步顺序：先修撞墙 → 最高分（localStorage）→ 每 5 个食物加速
- 约定：纯原生 JS 零依赖、不拆分三个源文件、file:// 直开可运行

**三份隔离副本**（下家 agent 只能看到自己那份输入，严格隔开）：

| 副本 | 下家能看到的全部输入 |
|---|---|
| m1-direct（直接交接） | 只有项目文件（4 个，共 2054 字节），无任何交接物 |
| m2-simple（简单文档交接） | 项目文件 + 一份随手写的 5 行交接文档（183 字节） |
| m3-skill（skill 交接） | 项目文件 + `.handoff/` 五件套 + AGENTS.md（skill v1.0.2 生成） |

## 二、下家 agent B 的三次接手表现

### B1（直接交接）：读完代码后的计划

> "项目结构清晰。我计划：① 实现暂停功能（空格键）；② 游戏结束与重开判定；③ 最高分；④ 加速；⑤ 撞墙判定似乎被注释了，我会排查原因后修复。"

**问题**：① 是**用户明确拒绝的功能**（跑偏）；⑤ 说明它不知道根因，要**重复排查**（skill 的 decisions.md 里一句话就能说清）；约定（零依赖/不拆文件/file://）只能靠猜。

### B2（简单文档交接）：读完文档和代码后的计划

> "文档说明确了三个待办：结束重开、最高分、加速。我按顺序做。另外我注意到没有暂停功能，会顺手加上。"

**问题**："顺手加暂停"再次踩中用户禁忌；bug 根因文档没写，仍要自己排查；约定全部缺失。

### B3（skill 交接包）：只读入口卡 + status + decisions 后的汇报

> "我理解的现状：能玩但不会死——snake.js 的 step() 里撞墙判定正确，但 gameOver() 未实现被注释；重开逻辑未写。我建议先做 T1：实现 gameOver（清场+结束提示）→ 出界/撞自己时调用 → 重开，验收是撞墙必死、能重开。之后按 T2 用 localStorage 做最高分。**不会加暂停**（用户已明确拒绝），保持零依赖、不拆文件、file:// 可跑。请确认我是否按此开始。"

**无跑偏、零重复排查、一步到位等待确认。**

## 三、量化对比

### 信息恢复度（下家能拿回多少上家"脑子里的东西"）

| 关键信息 | M1 直接 | M2 简单文档 | M3 skill |
|---|---|---|---|
| 当前进度 | 6/10（靠读代码推断） | 7/10 | 10 |
| bug 根因 | 3/10（需自行排查） | 3/10 | **10（直接给出位置+根因）** |
| 下一步优先级 | 4/10（自己定） | 6/10 | 10 |
| 用户禁忌（不要暂停） | 0（**必然跑偏**） | 0（**大概率跑偏**） | **10（禁忌块+用户原话双保险）** |
| 项目约定 | 5/10 | 5/10 | 10 |
| 用户原话 | 无 | 无 | **有** |

### 下家必读量（真实字节统计）

| | M1 | M2 | M3 |
|---|---|---|---|
| 理解项目状态需读 | 2054 B（全部代码） | 2237 B | 4339 B（3 份交接文件，未读代码） |
| 开工 T1 需读 | 2054 B **+ 未知时长的排查** | 2237 B + 排查 | 5866 B（含 snake.js，直接定位到行） |

**诚实发现**：在这个极小的测试项目上，M3 的"理解状态"读取字节（4339）**高于** M1（2054）——小项目本身读起来不贵，skill 的读取量优势体现不出来。skill 在这个维度上的真实收益是**省掉重复排查 bug 的时间和返工轮次**，而不是字节数。

**规模推演**：M1/M2 的读取成本随项目规模线性增长（真实项目几万~几百万字节必须全读才能理解状态），而 M3 恒定为"入口卡 1.3KB + 按需两三层"，**项目越大差距越大**。本实验项目过小，压低了 M3 在字节维度上的表现。

### 跑偏风险与返工（最核心差异）

| | M1 | M2 | M3 |
|---|---|---|---|
| 计划含"暂停功能"（用户禁忌） | 是 | 是（"顺手加"） | **否** |
| 重复排查已知 bug | 是 | 是 | 否 |
| 首轮计划可被直接确认 | 否（需用户纠正） | 存疑 | **是** |

## 四、结论

1. **skill 的核心价值不在"读得更少"（小项目上甚至略多），而在"不跑偏、不重复劳动、一次对齐"**——禁忌与用户原话的传递是 M1/M2 结构性做不到的，因为它们没有"约定与禁忌"这个专属位置。
2. 简单文档（M2）与直接交接（M1）的差距很小：一份没有结构约束的文档，天然会漏掉"为什么、禁忌、坑"这类最贵的信息。
3. 省 token 的优势随项目规模放大：项目越大，M1/M2 的全量阅读成本越高，M3 恒定轻量。

## 五、局限性（如实声明）

- 本实验为**同会话模拟**：A 的上下文是剧本，B 的隔离靠副本目录实现，非真实的跨会话/跨工具环境。
- 单项目样本（贪吃蛇）。结论如需复验，可用俄罗斯方块等项目重复本流程。
- B 的三次输出由同一执行者扮演，存在扮演者偏差；真实多 agent 测试才能完全消除。
- 交接包由 skill 规则生成，其质量依赖上家填写的认真程度。
