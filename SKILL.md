---
name: project-handoff
description: "当一个 agent 在某个项目上工作很久、要把项目转交给另一个 agent（可能是不同工具）时使用。生成结构化的项目交接包（分层：入口卡、状态、决策与禁忌、能力、索引），让下一个 agent 快速接手、尽量还原上一个 agent 的能力；接手方也能快速读懂项目和用户需求。特点：省 token（分层按需加载、索引不搬原文、skill 自身轻量）、速度快（首次全量生成、之后增量维护活档案）、信息完整（未完成任务带目的背景现状下一步验收优先级并挂靠项目目标，含给新 agent 的接手指令）。触发：交接项目、准备交接、生成交接包、把项目交给下一个 agent、handoff、接手项目、继续这个项目、快速上手这个项目、onboard。"
version: 1.0.2
agent_created: true
---

# 项目交接

## 什么时候用

- 一个 agent 在某个项目上工作了一段时间，要转交给另一个 agent（可能是不同工具，如从 WorkBuddy 换到 Claude Code、Cursor、Cline）时。
- 用户说：交接项目、准备交接、生成交接包、帮我把项目交给下一个 agent、handoff。
- 接手方说：接手项目、继续这个项目、快速上手这个项目、onboard。
- 已在项目上工作的 agent 收尾时说：收工、这个任务做完了、今天到这、先记一下进度。
- 也适用于同一个 agent 长时间项目的中断续接（今天干到一半，明天接着干）。

不用来：写普通项目 README（属文档类）、单纯总结项目（属总结类）、给新人看的入职材料（面向人不是 agent）。

## 总纲：交接包是"活的档案"

交接包不是一次性交接文档，而是这个项目的长期状态档案。正确形态：

1. 首次交接：完整生成一次（这是唯一一次相对高成本的全量扫描）。
2. 日常：每次收尾（用户说"收工 / 这个任务做完了 / 今天到这"等）顺手更新状态文件，保持新鲜。
3. 再次交接：只增量刷新过期块，秒级完成。

交接包永远在目标项目根目录下，不在本 skill 自己的目录里。

## 交接包放在哪、长什么样

交接包生成在目标项目根目录下，全部是纯文本，任何 agent 都能读：

```text
<项目根>/
├── .handoff/
│   ├── HANDOFF.md      入口卡（唯一必读，几百字）
│   ├── status.md       状态快照（进度、未完成任务、下一步）
│   ├── decisions.md    决策、约定与禁忌、踩过的坑
│   ├── capabilities.md 项目命令与工作流、上家 agent 工具
│   └── index.md        关键文件索引（只记路径，不搬原文）
└── <入口跳板>           按目标工具决定（AGENTS.md / CLAUDE.md / .cursorrules 等）
```

完整模板和填写规范见 @references/handoff-templates.md（仅交接模式加载）。

## 交接模式（你是要交出的那一个）

触发词：交接项目、准备交接、生成交接包、帮我把项目交给下一个 agent、handoff。

1. 加载 @references/handoff-templates.md。
2. 上下文自检：判断本次会话是否真的带着这个项目的工作上下文（做过什么、卡在哪、为什么）。
   - 有：继续第 3 步，交接信息以会话上下文为主、扫描为辅。
   - 没有（会话刚开启就交接、或只是替别人代交）：明确告知用户"正在做降级交接：代码和文件状态是准的，但'为什么、卡在哪'这类上下文信息会缺失"，再继续，并在交接包 HANDOFF.md 的元信息里标注"降级交接"。
3. 检测交接包是否已存在（`.handoff/` 目录）：
   - 不存在：进入完整生成（第 4 步）。
   - 已存在：进入增量刷新（第 5 步）。
4. 完整生成（首次），用轻量扫描，不全量读代码：
   1. 先读入口：README、构建/依赖配置（package.json、requirements.txt、pom.xml 等）、顶层目录结构、最近的改动。
   2. 按需深入：只深入接手必需的 1 到 3 个核心模块，不逐个读所有源码文件。
   3. 结合本项目已有的记忆、日志、文档（如果有）。
   4. 按模板生成 `.handoff/` 下 5 个文件和入口跳板。
5. 增量刷新（已有交接包）：逐文件检查各块是否过期，只刷新过期或新增的部分（主要动 status.md；决策、能力、索引变了也要同步刷），不重写未变化的内容。
5. 问一句目标工具：确认下一个 agent 是什么工具，生成或更新对应入口跳板文件，内容就是一句话指向 `.handoff/HANDOFF.md`：
   - Claude Code 用 `CLAUDE.md`
   - Cursor 用 `.cursorrules`（或 `.cursor/rules/`）
   - 其他或不确定用 `AGENTS.md`
6. 敏感信息兜底：扫描中遇到 .env、密钥、token、密码、私钥，只记"存在该文件"，绝不复制其值进交接包。
7. 跑完整性自查（见下文"交接模式自查"），逐条核对。
8. 输出成本报告：入口卡 X 字、全量 T 字、新 agent 接手预估读 N 字，让"省了多少 token"看得见。
9. 产出"给新 agent 的开场提示词"，让用户直接复制发给新 agent，例如：
   "这个项目有交接包。请先读 `.handoff/HANDOFF.md`，并按里面的接手指令操作。"
10. 提示（不自动执行）：建议把 `.handoff/` 和入口跳板 commit 进版本控制，跨机器不丢。不自动 commit。

## 接手模式（你是刚接手的那一个）

触发词：接手项目、继续这个项目、快速上手这个项目、onboard。

1. 加载 @references/onboarding-playbook.md，按其操作手册执行。核心顺序：
   1. 先读入口卡 `.handoff/HANDOFF.md`（含给新 agent 的接手指令）。
   2. 对齐项目目标，再按入口卡指引，只读与当前任务相关的层（每个文件第一行都写着"何时读我"）。
   3. 检查新鲜度（各文件头的更新时间），过期或和实际项目对不上时提醒用户"建议重新交接"。
2. 理解自检：能自答下面 4 个问题才算读懂，答不上就回去读对应层：
   - 项目目标是什么？当前阶段目标是什么？
   - 当前最优先的未完成任务是哪个？
   - 这个任务的目的、背景、验收标准是什么？
   - 有哪些约定与禁忌必须遵守？
3. 汇报并等确认：用几句话汇报"我理解的现状 + 我建议先做的下一步"，等用户确认后再动手。绝不未经确认就开始改。
4. 确认后继续干活，并把 status.md 当作活的档案持续更新。

## 绝不做什么

- 不把代码、大文件、长日志的原文复制进交接包，只在 index.md 记路径和关键行。
- 不把密钥、token、密码、私钥等敏感值写进交接包，只记"存在该文件"。
- 不删除、不覆盖项目里交接包之外的任何已有文件。
- 不自动执行 commit、部署、发送消息等改变状态的操作。
- 接手后不在用户确认前就开始改代码。

## 交接模式自查

生成或刷新完成后逐条核对，任何一条不满足就补全再交付：

- [ ] `.handoff/` 下 5 个文件齐全，入口跳板已生成。
- [ ] HANDOFF.md 是入口卡：几百字，含元信息、现状、给新 agent 的接手指令。
- [ ] 每个未完成任务有七要素（任务/目的/背景/现状/下一步/验收/优先级），且挂靠项目目标。
- [ ] decisions.md 含"约定与禁忌"块。
- [ ] 每个文件第一行写着"何时读我"，文件头有更新时间。
- [ ] 无敏感值泄露，大文件只记路径。
- [ ] 成本报告和开场提示词已输出。
