# 俄罗斯方块对比测试报告（复验）

> 实验时间：2026-09-22 ｜ 对象：project-handoff skill v1.0.2 ｜ 与贪吃蛇实验同方法，换项目、换烂尾点、换禁忌复验

## 一、实验设置

**测试项目**：原生 JS 俄罗斯方块，半成品——能移动/旋转/下落/触底固定；烂尾点换成：消行有 bug（`clearLines()` 里 splice 后缺 `y--` 回退，连消漏行）、游戏结束未写、计分未写、下一个预览未写。

**上家 A 的隐藏上下文（剧本，不写进项目文件）**：
- 用户指定计分规则："消一行 100、两行 300、三行 500、四行 800；**别整花里胡哨的落地虚线，看不清**"
- bug 根因：clearLines 里 splice 后没 y-- 回退（遍历中改数组的典型坑）
- 下一步顺序：修消行 → 游戏结束 → 计分 → 下一个预览
- 约定：纯原生 JS 零依赖、不拆分三文件、不引 CDN

**三份隔离副本**：tetris-m1（直接）、tetris-m2（简单文档）、tetris-m3（skill 交接包）。

## 二、三次接手表现

**B1（直接交接）**：读完代码后计划——①消行（自己排查"为什么连消漏行"，不知道 y-- 回退根因）；②计分（自己定规则，如"每行 100 分"，不知道连消递增）；③下一个预览；④**加落地位置预览虚线（ghost piece）**；⑤游戏结束。**踩中两个禁忌/盲区：加 ghost piece（用户不要）、自定计分规则（用户已指定）。**

**B2（简单文档）**：文档列了"消行、结束、计分、预览"四个待办，照做；但计分自己定、可能加 ghost piece、bug 根因要自己排查。

**B3（skill）**：只读入口卡 + status + decisions，汇报——"先修 T1：clearLines 的 splice 后补 y-- 回退；再 T2 结束判定；T3 按用户指定经典规则计分（100/300/500/800）；T4 预览。**不加落地虚线（用户明确不要）**、保持零依赖。"——直达根因、守住禁忌、一次对齐。

## 三、量化对比

### 信息恢复度

| 关键信息 | M1 | M2 | M3 |
|---|---|---|---|
| 当前进度 | 6/10 | 7/10 | 10 |
| bug 根因（splice y-- 回退） | 3/10 | 3/10 | **10** |
| 计分规则（用户指定） | 0（自定） | 0（自定） | **10** |
| 禁忌（不要 ghost piece） | 0（**跑偏**） | 0（**可能跑偏**） | **10** |
| 下一步优先级 | 4/10 | 6/10 | 10 |
| 项目约定 | 5/10 | 5/10 | 10 |

### 下家必读量（真实字节）

| | M1 | M2 | M3 |
|---|---|---|---|
| 理解状态需读 | 3506 B（全读） | 3698 B | 5004 B（3 份交接文件） |
| 开工 T1 需读 | 3506 B + 排查 | 3698 B + 排查 | 7904 B（含 tetris.js） |

规律与贪吃蛇一致：小项目上 M3 字节不占优，价值在**不跑偏 + 不重复排查 + 一次对齐**；字节优势随项目规模放大。

## 四、结论（两次实验一致）

1. **skill 的核心价值再次验证在"禁忌与用户原话的传递"**——两次实验里 M1/M2 都结构性踩中了用户明确拒绝的功能（贪吃蛇的暂停键、俄罗斯方块的 ghost piece），因为它们没有"约定与禁忌"这个专属位置。
2. **bug 根因直达**避免了两次实验里 M1/M2 都发生的"重复排查已知问题"。
3. 换项目、换烂尾点、换禁忌，结论不变，说明**结论具有一定普遍性**，而非单个项目巧合。

## 五、局限性（同贪吃蛇，如实）

- 同会话模拟 + 单执行者扮演，非真实跨会话/跨工具。
- 两项目均为小规模前端游戏；对中大型项目、多文件项目，结论待更大样本验证（可预期字节优势更明显）。
- 交接包质量依赖上家填写认真程度。
